<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Permissions -->
    <uses-permission
        android:name="android.permission.CAMERA"
        tools:ignore="PermissionImpliesUnsupportedChromeOsHardware" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"
        tools:ignore="ScopedStorage" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/penny_logo"
        android:label="Penny-Wize"
        android:roundIcon="@mipmap/penny_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.PennyWize"
        tools:targetApi="31">

        <!-- Splash Activity (Launcher) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.PennyWize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Main Login Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false" /> <!-- Register Activity -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false" /> <!-- Home Activity -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" /> <!-- Add Expense Activity -->
        <activity
            android:name=".AddExpenseActivity"
            android:exported="false" /> <!-- View Expenses Activity -->
        <activity
            android:name=".ViewExpensesActivity"
            android:exported="false" /> <!-- Categories Activity -->
        <activity
            android:name=".CategoriesActivity"
            android:exported="false" /> <!-- Goals Activity -->
        <activity
            android:name=".GoalsActivity"
            android:exported="false" /> <!-- Category Totals Activity -->
        <activity
            android:name=".CategoryTotalsActivity"
            android:exported="false" /> <!-- FileProvider for camera photos -->
        <activity
            android:name=".AnalyticsActivity"
            android:exported="false" /> <!-- Analytics Activity -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.pennywize.app.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>